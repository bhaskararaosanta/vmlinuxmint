#include<stdio.h>

int main()
{
	int a = 0, b = 1, c = 2, d = 3;
	//if(b, c, d, a)
	// The above if condition will evaluate from left to right and finally execute a value and its 0 and become false
	if(a, b, c, d)
	printf("%d\t%d\t%d\t%d\n", a, b, c, d);

	return 0;
}
